from django.db import models
# from django.forms import CharField

# class Usuario(models.Model):
#     usuario = CharField(max_length=40,null=True, blank=True)
#     contraseña = CharField(max_length=40,null=True, blank=True)
